# Playwright Learning

This repository holds scripts and code from my journey of learning Playwright for browser automation and end-to-end testing.

## Playwright Setup
This project uses Node.js and Playwright.

To get started with local development:
1. Clone this repository
2. Run `npm install` to install dependencies
3. Run `npx playwright test` to run the tests
